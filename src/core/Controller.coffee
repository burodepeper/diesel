
# Controller
# -----------------------------------------------------------------------------
# An empty abstraction for an {Entity} that is intended to be used as a parent
# for any and all controllers. A controller can be thought of as an entity that
# has no direct visual representation.

class Controller extends Entity
  
  constructor: ->
    super()
